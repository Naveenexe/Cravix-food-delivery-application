<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "https://hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Database connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/cravix</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">12341234</property>

        <!-- SQL Dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Show SQL in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Session context -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Don't let Hibernate create or modify tables -->
        <property name="hibernate.hbm2ddl.auto">validate</property>

        <!-- Entity mappings -->
        <mapping class="com.cravix.model.User"/>
        <mapping class="com.cravix.model.Restaurant"/>
        <mapping class="com.cravix.model.MenuItem"/>
        <mapping class="com.cravix.model.Orders"/>
        <mapping class="com.cravix.model.OrderItem"/>

    </session-factory>
</hibernate-configuration>